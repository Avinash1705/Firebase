<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.searchkaro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/serach"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminLoginActivity"></activity>
        <activity android:name=".AdminPower.Admin_contact" />
        <activity android:name=".AdminPower.Admin_newItem" />
        <activity android:name=".AdminPower.ChooseAdminPage" />
        <activity android:name=".SearchKaro" />
        <activity android:name=".Contact" />
        <activity android:name=".Advertise_User" />
        <activity android:name=".NewItemActivity" />
        <activity android:name=".FiveActivityPage" />
        <activity android:name=".registrationActivity" />
        <activity android:name=".otpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>